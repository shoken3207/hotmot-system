import {
  ce,
  gebi,
  ac,
  addClasses,
  removeClass,
  setSrc,
  setHref,
  setValue,
  showToast,
  rlc,
} from "../js/utils.js";

const orderDetailsEl = gebi("orderHistory");
window.addEventListener("DOMContentLoaded", () => {
	console.log(orderDetailsEl)
})